[project]
name = "oxzoo-fastapi-react"
version = "0.1.0"
description = "ox deploy example: FastAPI API + React SPA"
requires-python = ">=3.13"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn==0.53.0",
]

[tool.uv]
package = false
